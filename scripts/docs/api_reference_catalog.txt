# 11. API Reference

Generated from runtime signatures and source docstrings as portable Markdown so the same reference is readable in the repository, on the documentation site, and in the wiki. Sections are ordered from foundational APIs to specialized modules.

## 1. Top-level package

@api nnx members=__version__,set_seed,dataloader_worker_init_fn,env_snapshot,LRFinderResult,lr_finder

## 2. Orchestrators

### 2.1. NNModel — supervised orchestrator

@api nnx.nn.nn_model.NNModel

@api nnx.nn.nn_model.PredictResult

@api nnx.nn.nn_model.TrainStepContext

@api nnx.nn.nn_model.TrainStepFn

@api nnx.nn.nn_model.default_train_step

@api nnx.nn.nn_model.EvalStepContext

@api nnx.nn.nn_model.EvalStepFn

### 2.2. GenerativeNNModel — decoder-only LM orchestrator

@api nnx.nn.generative_nn_model.GenerativeNNModel

### 2.3. Trainer — multi-optimizer orchestrator

@api nnx.trainer.trainer.Trainer

@api nnx.trainer.trainer.TrainerStepContext

@api nnx.trainer.trainer.TrainerStepFn

@api nnx.trainer.params.NNTrainerParams

@api nnx.trainer.params_builder.NNTrainerParamsBuilder

## 3. Params

@api nnx.nn.params.nn_params.NNParams

@api nnx.nn.params.nn_model_params.NNModelParams

@api nnx.nn.params.nn_train_params.NNTrainParams

@api nnx.nn.params.nn_optim_params.NNOptimParams

@api nnx.nn.params.nn_optim_params_builder.NNOptimParamsBuilder

@api nnx.nn.params.nn_scheduler_params.NNSchedulerParams

@api nnx.nn.params.nn_scheduler_params_builder.NNSchedulerParamsBuilder

@api nnx.nn.params.nn_transformer_params.NNTransformerParams

@api nnx.nn.params.nn_transformer_params_builder.NNTransformerParamsBuilder

@api nnx.nn.params.nn_conv_params.NNConvParams

@api nnx.nn.params.nn_moe_params.NNMoEParams

@api nnx.nn.params.nn_tokenizer_params.NNTokenizerParams

@api nnx.nn.params.nn_tokenizer_params.train_bpe

@api nnx.nn.params.nn_run.NNRun

@api nnx.nn.params.nn_checkpoint.NNCheckpointTransform

@api nnx.nn.params.nn_checkpoint.NNCheckpoint

@api nnx.nn.params.nn_iteration_data_point.NNIterationDataPoint

@api nnx.nn.params.nn_evaluation_data_point.NNEvaluationDataPoint

## 4. Networks

@api nnx.nn.net.feed_fwd_nn.FeedFwdNN

@api nnx.nn.net.feed_fwd_moe_nn.FeedFwdMoENN

@api nnx.nn.net.conv_nn.ConvNN

@api nnx.nn.net.graph_nn_base.GraphNNBase

@api nnx.nn.net.graph_conv_nn.GraphConvNN

@api nnx.nn.net.graph_sage_nn.GraphSageNN

@api nnx.nn.net.graph_att_nn.GraphAttNN

@api nnx.nn.net.transformer_nn.TransformerNN

@api nnx.nn.net.vit_nn.ViTNN

@api nnx.nn.net.vit_nn.ViTBlock

@api nnx.nn.moe.MoELinear

## 5. Datasets

@api nnx.nn.dataset.nn_dataset_base.NNDatasetBase

@api nnx.nn.dataset.nn_dataset.NNDataset

@api nnx.nn.dataset.nn_graph_dataset.NNGraphDataset

@api nnx.nn.dataset.nn_tabular_dataset.NNTabularDataset

@api nnx.nn.dataset.nn_preference_dataset.NNPreferenceDataset

## 6. Enums

@api nnx.nn.enum.activations.Activations

@api nnx.nn.enum.checkpoints.Checkpoints

@api nnx.nn.enum.devices.Devices

@api nnx.nn.enum.losses.Losses

@api nnx.nn.enum.nets.Nets

@api nnx.nn.enum.optims.Optims

@api nnx.nn.enum.schedulers.Schedulers

## 7. Callbacks

@api nnx.nn.callbacks.Callback

@api nnx.nn.callbacks.EarlyStopping

@api nnx.nn.callbacks.LRMonitor

@api nnx.nn.callbacks.ModelCheckpoint

@api nnx.nn.callbacks.TensorBoardCallback

@api nnx.nn.callbacks.WandbCallback

## 8. Fine-tuning (`nnx.finetune`)

@api nnx.finetune.freezing.freeze

@api nnx.finetune.freezing.unfreeze

@api nnx.finetune.freezing.frozen

@api nnx.finetune.loading.load_pretrained

@api nnx.finetune.loading.LoadPretrainedResult

@api nnx.finetune.param_groups.NNParamGroupSpec

@api nnx.finetune.param_groups.build_param_groups

## 9. Parameter-efficient fine-tuning (`nnx.peft`)

LoRA + DoRA + IA3 + Prefix-Tuning + Prompt-Tuning + Adapters. All methods share the same in-place wrap + save/load idiom (per-method `save_*_weights` / `load_*_weights` persist only the trainable delta).

### 9.1. LoRA

@api nnx.peft.lora.LoRALinear

@api nnx.peft.lora.apply_lora_to

@api nnx.peft.lora.save_lora_weights

@api nnx.peft.lora.load_lora_weights

### 9.2. DoRA

@api nnx.peft.dora.DoRALinear

@api nnx.peft.dora.apply_dora_to

### 9.3. IA3

@api nnx.peft.ia3.IA3Linear

@api nnx.peft.ia3.apply_ia3_to

@api nnx.peft.ia3.save_ia3_weights

@api nnx.peft.ia3.load_ia3_weights

### 9.4. Prefix Tuning

@api nnx.peft.prefix.PrefixTuner

@api nnx.peft.prefix.save_prefix_weights

@api nnx.peft.prefix.load_prefix_weights

### 9.5. Prompt Tuning

@api nnx.peft.prompt.PromptTuner

@api nnx.peft.prompt.save_prompt_weights

@api nnx.peft.prompt.load_prompt_weights

### 9.6. Adapters

@api nnx.peft.adapters.AdapterLayer

## 10. Pruning (`nnx.prune`)

@api nnx.prune.magnitude.magnitude_prune

@api nnx.prune.semi_structured.semi_structured_24

## 11. Model surgery (`nnx.surgery`)

Walkthrough at [Model surgery](surgery.md). Every primitive returns a fresh `nn.Module` and composes with `NNModel.train()` for the "load checkpoint → surgery → refine" loop.

@api nnx.surgery.widen.widen

@api nnx.surgery.deepen.deepen

@api nnx.surgery.drop_layer.drop_layer

@api nnx.surgery.low_rank.low_rank_factorize

@api nnx.surgery.embedding.expand_embedding

## 12. Quantization (`nnx.quantize`)

PTQ INT8 weight-only + QAT 8da4w via [`torchao`](https://github.com/pytorch/ao) (the replacement for the removed `torch.ao.quantization`). Opt-in via `pip install "thekaveh-nnx[quantize]"`.

@api nnx.quantize.ptq.quantize_int8

@api nnx.quantize.qat.qat_train_step_factory

@api nnx.quantize.qat.QATLifecycleCallback

## 13. Diffusion (`nnx.diffusion`)

@api nnx.diffusion.schedules.NoiseSchedulers

@api nnx.diffusion.schedules.NoiseSchedule

@api nnx.diffusion.nets.DiffusionMLP

@api nnx.diffusion.nets.sinusoidal_time_embed

@api nnx.diffusion.training.diffusion_train_step_factory

@api nnx.diffusion.sampling.sample

## 14. Training paradigms (`nnx.paradigms`)

Each factory returns a `TrainStepFn` for the `train_step_fn=` hook on `NNModel.train`. The training loop, checkpoint cadence, callbacks, and persistence are unchanged — only the per-batch update is swapped.

### 14.1. Knowledge distillation

@api nnx.paradigms.distillation.kd_train_step_factory

@api nnx.paradigms.distillation.feature_kd_train_step_factory

@api nnx.paradigms.born_again.born_again_train

### 14.2. Contrastive

@api nnx.paradigms.contrastive.simclr_train_step_factory

@api nnx.paradigms.contrastive.nt_xent_loss

### 14.3. Augmentation

@api nnx.paradigms.augmentation.mixup_train_step_factory

@api nnx.paradigms.augmentation.cutmix_train_step_factory

### 14.4. Mixture-of-Experts

`MoELinear` is the drop-in layer (documented in §4); `moe_train_step_factory` adds the Switch-style load-balancing aux loss to the supervised step.

@api nnx.paradigms.moe.moe_train_step_factory

### 14.5. I-JEPA

Walkthrough at [I-JEPA](jepa.md). The `ViTNN` encoder is documented in §4.

@api nnx.paradigms.jepa.jepa_train_step_factory

@api nnx.paradigms.jepa.JEPAPredictor

@api nnx.paradigms.jepa.build_target_encoder

@api nnx.paradigms.jepa.update_ema

@api nnx.paradigms.jepa.random_block_mask

### 14.6. DPO

Walkthrough at [DPO](dpo.md).

@api nnx.paradigms.dpo.dpo_train_step_factory

## 15. Embeddings (`nnx.embeddings`)

End-to-end walkthrough at [Embeddings](embeddings.md). Opt-in via `pip install "thekaveh-nnx[embeddings]"`.

@api nnx.embeddings.contrastive_trainer.ContrastiveTextDataset

@api nnx.embeddings.contrastive_trainer.train_contrastive

@api nnx.embeddings.contrastive_trainer.embed_texts

@api nnx.embeddings.contrastive_trainer.text_contrastive_train_step_factory

@api nnx.embeddings.faiss_export.export_to_faiss

@api nnx.embeddings.faiss_export.export_to_safetensors

## 16. Interop (`nnx.interop`)

### 16.1. Experimental GGUF export

Walkthrough and stock-runtime limitations at [Experimental GGUF export](gguf.md). Opt-in via `pip install "thekaveh-nnx[gguf-write]"`.

@api nnx.interop.gguf.writer.write_gguf

@api nnx.interop.gguf.tensor_name_map.map_tensors

@api nnx.interop.ollama.export_ollama_modelfile

## 17. HuggingFace Hub + safetensors

Opt-in via `pip install "thekaveh-nnx[hub]"`. Two integration surfaces:

- **safetensors checkpoints** — `NNCheckpoint.to_file(..., format="safetensors")` and `NNCheckpoint.from_file(..., format="safetensors")` (see §3 `NNCheckpoint`) read and write checkpoints in the safetensors format alongside the default pickle path. Loadable by outside-Python tools (ComfyUI, vLLM, AutoGPTQ).
- **Hub publish / load** — `NNModel` mixes in `huggingface_hub.PyTorchModelHubMixin`, so `save_pretrained(local_dir)`, `push_to_hub(repo_id)`, and `NNModel.from_pretrained(repo_id)` work directly on a trained model. The mixin methods are inherited and live on `NNModel` itself — see §2.1.

Walkthrough at [HuggingFace Hub](hub.md).

## 18. Generation (`nnx.generation`)

`LogitsProcessor` chain for autoregressive sampling. Used by `GenerativeNNModel.generate()` (§2.2). Pure-torch — no optional deps.

@api nnx.generation.LogitsProcessor

@api nnx.generation.LogitsChain

@api nnx.generation.LogitsChainBuilder

@api nnx.generation.TemperatureScaling

@api nnx.generation.TopKFilter

@api nnx.generation.TopPFilter

@api nnx.generation.RepetitionPenalty

@api nnx.generation.apply_chain

@api nnx.generation.sample_next_token

## 19. Visualization

### 19.1. Run-output viz (`nnx.vis_utils`)

@api nnx.vis_utils members=confusion_matrix,classification_report,multi_line_plot,scatter_plot,two_dim_tsne_checkpoint_logits

### 19.2. Model-internals viz (`nnx.viz`)

Opt-in via `pip install "thekaveh-nnx[viz]"` (pulls `torchinfo` + `captum`) and `pip install "thekaveh-nnx[viz-interactive]"` (adds the `netron` browser viewer for `nnx.viz.netron_export(..., launch=True)`).

@api nnx.viz.activation.activation_map

@api nnx.viz.attribute.attribute

@api nnx.viz.gradient_flow.gradient_flow

@api nnx.viz.netron.netron_export

@api nnx.viz.summary.summary

@api nnx.viz.weight_histogram.weight_histogram

## 20. Utilities

@api nnx.utils members=print_tree,print_table,flatten_dict

### 20.1. `Utils` back-compat facade

`nnx.Utils` is a thin staticmethod facade over the module-level functions above, kept so existing notebook code that calls `Utils.print_tree(...)` / `Utils.print_table(...)` / `Utils.flatten_dict(...)` continues to work. New code should prefer the module-level functions directly.
